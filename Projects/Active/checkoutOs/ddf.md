
❌ NOT DONE (7 items left)
#	Plan Phase	Item	Notes
1	4	Create idempotency middleware — src/middleware/idempotency.middleware.ts	Responsibilities: extract Idempotency-Key header, validate UUID v4 (use uuid's validate), generate SHA-256 hash of method + path + body, attach to req.idempotency = { key, requestHash }. Must NOT call services or Redis (per AGENTS.md §4.2). Throw IdempotencyKeyMissingError / IdempotencyKeyInvalidError on bad input.
2	5	Modify payment controller — src/controllers/payments.controller.ts	Replace createPayment(req.body) with createPaymentWithIdempotency(req.body, req.idempotency). Keep asyncHandler wrapper + res.status(201).json(success(result)) pattern. Import '../types/express.types' so req.idempotency type-checks.
3	6	Register middleware on route — src/routes/payments.routes.ts	Mount idempotencyMiddleware ONLY on the create path: paymentRouter.post('/', idempotencyMiddleware, create). Other routes (GET /:chkId, POST /:chkId/refund) unchanged. Middleware must run AFTER express.json() so req.body is parsed before hashing — this is already the case because the router is mounted after the JSON parser in app.ts:78,81.
4	8	Store unit tests — tests/unit/store/idempotency.store.test.ts	Create the new tests/unit/store/ directory. Use the existing ioredis-mock (already loaded globally by vitest.setup.unit.ts:22-27). Cover: set+get round-trip, set NX refuses overwrite, Lua updateIdempotencyRecord rejects non-existent key, Lua forceUpdateIfInProgress only overwrites when status is IN_PROGRESS.
5	8	Service unit tests — tests/unit/services/idempotency.service.test.ts	Use vi.spyOn(IdempotencyStore, '<fn>') to mock the store. Cover all 5 paths of checkIdempotency: MISS (new), HIT (completed), IN_PROGRESS (fresh), hash mismatch → throws IdempotencyKeyReusedError, stale recovery → MISS, race exhausted → IN_PROGRESS. Cover completeIdempotency: normal update, missing-existing → create-new, hash mismatch → false + logs error, throws internally → false + never propagates.
6	8	Stale tests (the plan splits these into a separate file — tests/unit/services/idempotency.service.stale.test.ts)	Use vi.useFakeTimers() + vi.advanceTimersByTime(31000) to test the 30s staleness threshold edge: 29s → IN_PROGRESS, 31s → MISS via atomic recovery. Verify forceUpdateIfInProgress is called exactly once.
7	Integration tests — tests/integration/idempotency.test.ts	Use createTestApp() from tests/helpers/app.helper.ts + supertest against the singleton. Real Redis (flush chk:* already done in vitest.setup.integration.ts:61-69). Mock axios per-file (per existing payment.api.test.ts pattern). Test scenarios from the plan §10.5 checklist: (a) same req twice → same response, (b) different payload same key → 400 IDEMPOTENCY_KEY_REUSED, (c) parallel requests → one 201, one 409 REQUEST_IN_PROGRESS, (d) missing header → 400 MISSING_IDEMPOTENCY_KEY, (e) invalid UUID → 400 INVALID_IDEMPOTENCY_KEY, (f) different amount same orderId → 409 ORDER_ID_AMOUNT_MISMATCH, (g) different currency same orderId → 409 ORDER_ID_CURRENCY_MISMATCH, (h) stale IN_PROGRESS (30s) auto-recovery. Important assertion fix: plan says expect 200, but the existing controller returns 201 — match the actual contract.	 
8	9	Create ADR — docs/adr/001-idempotency.md	The docs/adr/ directory exists with 002-paytm-integration.md; slot 001 is vacant. Document: Context, Decision (Option A — Pure Service Layer), Consequences, Alternatives Considered (Option B rejected — violates §5.7), the 7 production fixes applied, "What's deferred to V1.2" (merchant namespace, metrics, DLQ). Set acceptance date to 2026 (not 2024 as the plan template said — that was a typo).
9	9	Update OpenAPI — openapi.yaml (project root, NOT docs/openapi.yaml — the plan had the wrong path)	Add Idempotency-Key header parameter to the POST /payments operation (required: true, schema: { type: string, format: uuid }). Add 400 response codes for MISSING_IDEMPOTENCY_KEY and INVALID_IDEMPOTENCY_KEY. Add 409 responses for REQUEST_IN_PROGRESS, ORDER_ID_AMOUNT_MISMATCH, ORDER_ID_CURRENCY_MISMATCH.
Note: src/utils/hash.ts is marked "optional" in the plan — the middleware can inline a 4-line createHash('sha256') without needing a separate util file. I'd skip it unless you want pure-function testability of the hash.