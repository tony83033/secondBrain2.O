resp is a array of string

Redis use RESP as a request response protocol
it support int , string array char, in RESP every datatype start with a special character and the data end with CRLF (Carriage return line fe)

